[workspace]
# Create a GitHub Release (with notes pulled from CHANGELOG.md) when a new
# version is published. Versions and changelogs are bumped by hand before
# pushing to master; the `Release-plz` workflow only publishes.
git_release_enable = true
changelog_update = false
